import sys, os
import csv
from pathlib import Path

for root, subdirectories, files in os.walk("./"):
    for subdirectory in subdirectories:
        for root, subdirectories, files in os.walk()

